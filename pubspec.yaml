name: mappa_operis
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.1-bleedingEdge+a99fc78a

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.5.1
  fluent_ui: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/audio/
